<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:albums="clr-namespace:PhotoGallery.App.Albums"
                    xmlns:people="clr-namespace:PhotoGallery.App.People"
                    xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
                    xmlns:shell="clr-namespace:PhotoGallery.App.Shell"
                    xmlns:theme="clr-namespace:PhotoGallery.App.Theme">

  <!-- Colours are DynamicResource throughout so a theme swap re-renders live. -->

  <!-- Segoe UI Variable Text is Windows 11's UI face and what the theme is
       specified in; plain Segoe UI is the fallback and is what Windows 10
       resolves to, so the app reads correctly on both. -->
  <FontFamily x:Key="Font.Ui">Segoe UI Variable Text, Segoe UI</FontFamily>
  <FontFamily x:Key="Font.Mono">Cascadia Mono, Consolas</FontFamily>

  <!--
    One radius vocabulary rather than a number per template, so a later control
    cannot quietly pick a seventh value.

    Inner is not a smaller radius chosen by eye: chrome nested inside other
    chrome stays concentric only when the inner radius is the outer one less the
    padding between them. The segmented toggle is the case in point - a 3px
    track with 2px of padding needs a 1px segment, or the two curves fight.

    Three rather than four because that is the Precision direction's corner, and
    Inner follows it down to keep the relationship above rather than staying at a
    number that was only ever correct against a 4px track.

    Static rather than Dynamic: a corner does not change with the theme.
  -->
  <CornerRadius x:Key="Radius.Control">3</CornerRadius>
  <CornerRadius x:Key="Radius.Inner">1</CornerRadius>

  <!--
    Anything that floats over the app is one of these, so there is one answer to
    what a modal looks like rather than one per place somebody needed one.

    Written as a style on Border rather than a control, because its users want
    different insides - a dialog holds a question and two buttons, the pass
    overlay a live progress bar, the pickers a filtering box over a list, the
    album panels a form - and what has to agree is the surface they sit on, not
    their contents.

    Size and placement are deliberately absent. All but one are centred inside
    the window they cover, and the dialog fills a window of its own, so those are
    the caller's to set; putting them here would only mean every caller
    overriding them. Padding is here because it is right for most of them, and
    the narrower panels override it - 24 around a question 320 wide reads as a
    box with something small lost in the middle of it.
  -->
  <Style x:Key="ModalSurface" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource Panel.Background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Panel.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{DynamicResource Radius.Control}" />
    <Setter Property="Padding" Value="24,22,24,18" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
  </Style>

  <!--
    The shade a modal is read against. Deliberately the same in both themes: a
    modal shade is a dimming of what is behind it, and dimming a light screen
    with a light wash would say nothing. It is the one colour here that does not
    come from the palette, and it is defined once so that the places needing it
    cannot drift - which, being written as a literal instead, they had: the two
    album panels were dimming with a plain 60% black while everything else used
    this, and a modal was a different darkness depending on which one it was.
  -->
  <SolidColorBrush x:Key="ModalScrim" Color="#CC1E1E1E" />

  <!--
    What a modal calls itself. A style rather than a FontSize written at each
    one, because written at each one they drifted: the pass overlay said 16 and
    the dialog borrowed Heading, which is 28 and bold and is what the Settings
    page titles itself with. A question arrived in the same type as the screen it
    was covering, wrapped to two lines, and dwarfed its own explanation.
  -->
  <Style x:Key="ModalTitle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="16" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="TextWrapping" Value="Wrap" />
  </Style>

  <!--
    Scrollbars. Both palettes have carried ScrollBarTrack, ScrollBarThumb and
    ScrollBarThumbHover since the theme landed and nothing had ever consumed
    them, so every ScrollViewer in the app fell through to Windows' own grey
    chrome - arrow buttons and all, pale on a dark panel. Six dead brushes and
    one obviously foreign control.

    6 DIP at rest, 12 on hover, from the handoff. The bar reserves 12 either way
    and paints into half of it at rest rather than growing the layout: a bar that
    widened on approach would reflow the text beside it out from under the
    pointer. That is the one departure from the handoff, which asks for a true
    overlay - that needs a ScrollViewer template, and an implicit one of those
    would reach the GridView header and the TextBox's PART_ContentHost, neither
    of which survives being retemplated by accident.
  -->
  <Style x:Key="ScrollPageStrip" TargetType="RepeatButton">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RepeatButton">
          <!-- Transparent rather than empty: this is the strip that pages when
               clicked, and a null background is not hit-testable. -->
          <Border Background="Transparent" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ScrollThumb" TargetType="Thumb">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Background" Value="{DynamicResource ScrollBarThumb}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Thumb">
          <Border Background="{TemplateBinding Background}"
                  CornerRadius="6" SnapsToDevicePixels="True" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="{x:Type ScrollBar}">
    <Setter Property="OverridesDefaultStyle" Value="True" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Width" Value="12" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ScrollBar}">
          <!-- The Grid is transparent so all 12 DIP answer the pointer while
               only 6 of them are painted. -->
          <Grid Background="Transparent">
            <Border x:Name="Groove" Width="6" Margin="0,2"
                    HorizontalAlignment="Right"
                    Background="{DynamicResource ScrollBarTrack}"
                    CornerRadius="6" SnapsToDevicePixels="True" />
            <Track x:Name="PART_Track" Width="6" Margin="0,2"
                   HorizontalAlignment="Right" IsDirectionReversed="True">
              <Track.DecreaseRepeatButton>
                <RepeatButton Style="{StaticResource ScrollPageStrip}"
                              Command="ScrollBar.PageUpCommand" />
              </Track.DecreaseRepeatButton>
              <Track.Thumb>
                <Thumb x:Name="Thumb" Width="4" HorizontalAlignment="Center"
                       Style="{StaticResource ScrollThumb}" />
              </Track.Thumb>
              <Track.IncreaseRepeatButton>
                <RepeatButton Style="{StaticResource ScrollPageStrip}"
                              Command="ScrollBar.PageDownCommand" />
              </Track.IncreaseRepeatButton>
            </Track>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Groove" Property="Width" Value="12" />
              <Setter TargetName="PART_Track" Property="Width" Value="12" />
              <Setter TargetName="Thumb" Property="Width" Value="8" />
              <Setter TargetName="Thumb" Property="Background"
                      Value="{DynamicResource ScrollBarThumbHover}" />
            </Trigger>
            <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
              <Setter TargetName="Thumb" Property="Background"
                      Value="{DynamicResource AccentBackground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="Orientation" Value="Horizontal">
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Height" Value="12" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ScrollBar}">
              <Grid Background="Transparent">
                <Border x:Name="Groove" Height="6" Margin="2,0"
                        VerticalAlignment="Bottom"
                        Background="{DynamicResource ScrollBarTrack}"
                        CornerRadius="6" SnapsToDevicePixels="True" />
                <Track x:Name="PART_Track" Height="6" Margin="2,0"
                       VerticalAlignment="Bottom">
                  <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollPageStrip}"
                                  Command="ScrollBar.PageLeftCommand" />
                  </Track.DecreaseRepeatButton>
                  <Track.Thumb>
                    <Thumb x:Name="Thumb" Height="4" VerticalAlignment="Center"
                           Style="{StaticResource ScrollThumb}" />
                  </Track.Thumb>
                  <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollPageStrip}"
                                  Command="ScrollBar.PageRightCommand" />
                  </Track.IncreaseRepeatButton>
                </Track>
              </Grid>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="Groove" Property="Height" Value="12" />
                  <Setter TargetName="PART_Track" Property="Height" Value="12" />
                  <Setter TargetName="Thumb" Property="Height" Value="8" />
                  <Setter TargetName="Thumb" Property="Background"
                          Value="{DynamicResource ScrollBarThumbHover}" />
                </Trigger>
                <Trigger SourceName="Thumb" Property="IsDragging" Value="True">
                  <Setter TargetName="Thumb" Property="Background"
                          Value="{DynamicResource AccentBackground}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- Converters live here, beside the template that uses them, so a
       StaticResource lookup resolves within this dictionary. -->
  <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
  <shell:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
  <shell:InverseBooleanConverter x:Key="InverseBool" />
  <shell:NullToVisibilityConverter x:Key="NullToVisibility" />
  <shell:InverseNullToVisibilityConverter x:Key="InverseNullToVisibility" />
  <shell:SectionEnabledConverter x:Key="SectionEnabled" />
  <shell:SectionToolTipConverter x:Key="SectionToolTip" />
  <shell:SectionCheckedConverter x:Key="SectionChecked" />
  <shell:SectionCountConverter x:Key="SectionCount" />
  <shell:RemainingWidthConverter x:Key="RemainingWidth" />

  <!-- Table styling. A ListView with a GridView is used rather than hand-built
       rows so the header and the rows share one set of column widths and cannot
       drift apart. -->
  <Style x:Key="TableColumnHeader" TargetType="GridViewColumnHeader">
    <Setter Property="Background" Value="{DynamicResource Neutral.2}" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Placeholder}" />
    <Setter Property="FontSize" Value="11.5" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Padding" Value="10,8" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="GridViewColumnHeader">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{DynamicResource Separator}" BorderThickness="0,0,0,1"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter
              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
              VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- For numeric columns, so the heading sits over its figures. -->
  <Style x:Key="TableColumnHeaderRight" TargetType="GridViewColumnHeader"
         BasedOn="{StaticResource TableColumnHeader}">
    <Setter Property="HorizontalContentAlignment" Value="Right" />
  </Style>

  <Style x:Key="TableRow" TargetType="ListViewItem">
    <Setter Property="Padding" Value="10,9" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListViewItem">
          <Border x:Name="Root" Background="Transparent"
                  BorderBrush="{DynamicResource Separator}" BorderThickness="0,0,0,1"
                  Padding="{TemplateBinding Padding}">
            <GridViewRowPresenter
              Content="{TemplateBinding Content}"
              Columns="{TemplateBinding GridView.ColumnCollection}"
              VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Table" TargetType="ListView">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource TableRow}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
  </Style>

  <Style x:Key="TableCell" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
  </Style>

  <!-- Shared by the side nav's top and bottom groups, so a section looks and
       behaves the same wherever it sits. Selection goes through a command
       rather than a click handler, because a ResourceDictionary has no
       code-behind to wire events to. -->
  <DataTemplate x:Key="NavigationItem">
    <ToggleButton Style="{DynamicResource NavigationItemButton}"
                  AutomationProperties.Name="{Binding Title}"
                  ToolTipService.ShowOnDisabled="True"
                  Command="{Binding DataContext.SelectSectionCommand,
                            RelativeSource={RelativeSource AncestorType=Window}}"
                  CommandParameter="{Binding}">
      <ToggleButton.Content>
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>

          <!-- A fixed cell, first in the row. Everything that changes between
               the two widths is to its right, so the icon cannot be pushed
               about mid-animation: nothing upstream of it in the layout moves,
               and the star column simply absorbs whatever the pane has left on
               each frame. That, rather than a pair of per-state paddings tuned
               to agree, is what holds it still.

               The icon font is on this TextBlock alone. On the button it would
               inherit onto the name and the count, and Segoe MDL2 Assets has no
               letters or digits at all, so both would fall silently through to
               WPF's font fallback. -->
          <TextBlock Grid.Column="0" Width="14" Text="{Binding Glyph}"
                     FontFamily="Segoe MDL2 Assets" FontSize="14"
                     TextAlignment="Center" VerticalAlignment="Center" />

          <!-- Collapsed, never Hidden, and never faded: a faded name still holds
               its width open. Driven by the window rather than by the view
               model's fold, because the two change at different moments - the
               names leave as a fold begins, and come back only once the pane has
               finished opening, or they would re-trim their own ellipsis for the
               whole 180ms on the way out. -->
          <TextBlock Grid.Column="1" Margin="10,0,0,0" Text="{Binding Title}"
                     FontFamily="{StaticResource Font.Ui}" FontSize="13"
                     VerticalAlignment="Center" TextTrimming="CharacterEllipsis"
                     Visibility="{Binding NavLabelsVisible,
                                  RelativeSource={RelativeSource AncestorType=Window},
                                  Converter={StaticResource BoolToVisibility}}" />

          <TextBlock Grid.Column="2" Margin="8,0,0,0" FontSize="11"
                     FontFamily="{StaticResource Font.Ui}"
                     VerticalAlignment="Center"
                     Foreground="{DynamicResource TextDisabled}"
                     Visibility="{Binding NavLabelsVisible,
                                  RelativeSource={RelativeSource AncestorType=Window},
                                  Converter={StaticResource BoolToVisibility}}">
            <TextBlock.Text>
              <MultiBinding Converter="{StaticResource SectionCount}">
                <Binding Path="Key" />
                <Binding Path="DataContext.Counts"
                         RelativeSource="{RelativeSource AncestorType=Window}" />
                <Binding Path="DataContext.Sources.Count"
                         RelativeSource="{RelativeSource AncestorType=Window}" />
              </MultiBinding>
            </TextBlock.Text>
          </TextBlock>
        </Grid>
      </ToggleButton.Content>
      <ToggleButton.IsChecked>
        <MultiBinding Converter="{StaticResource SectionChecked}" Mode="OneWay">
          <Binding />
          <Binding Path="DataContext.SelectedSection"
                   RelativeSource="{RelativeSource AncestorType=Window}" />
        </MultiBinding>
      </ToggleButton.IsChecked>
      <ToggleButton.IsEnabled>
        <MultiBinding Converter="{StaticResource SectionEnabled}">
          <Binding Path="RequiresSources" />
          <Binding Path="DataContext.HasSources"
                   RelativeSource="{RelativeSource AncestorType=Window}" />
          <Binding Path="RequiresFaces" />
          <Binding Path="DataContext.FacesAvailable"
                   RelativeSource="{RelativeSource AncestorType=Window}" />
        </MultiBinding>
      </ToggleButton.IsEnabled>
      <!-- One tooltip, not two. Folded, the icon is not a label and every item
           needs its name; open, only a disabled item has anything left to say -
           and it has to say it either way, because it is the reason the item
           will not respond. The name TextBlock deliberately has no tooltip of
           its own: WPF resolves the tooltip on the innermost element under the
           pointer, so one there would hide this exactly where the pointer
           lands. -->
      <ToggleButton.ToolTip>
        <MultiBinding Converter="{StaticResource SectionToolTip}">
          <Binding Path="Title" />
          <Binding Path="RequiresSources" />
          <Binding Path="DataContext.HasSources"
                   RelativeSource="{RelativeSource AncestorType=Window}" />
          <Binding Path="DataContext.IsNavCollapsed"
                   RelativeSource="{RelativeSource AncestorType=Window}" />
          <Binding Path="RequiresFaces" />
          <Binding Path="DataContext.FacesAvailable"
                   RelativeSource="{RelativeSource AncestorType=Window}" />
        </MultiBinding>
      </ToggleButton.ToolTip>
    </ToggleButton>
  </DataTemplate>

  <!--
    32 high in both states, and every horizontal measurement in it is
    state-independent: the rail is overlaid rather than laid out, so it costs the
    content no width, and the presenter's 7 puts the icon's left edge 19 DIP from
    the pane's edge whether the names are there or not. Centre 26, which is half
    of the 52 the pane folds to.

    Foreground is set once on the button and inherits down to the glyph and the
    name, so a state trigger only has to touch one property.
  -->
  <Style x:Key="NavigationItemButton" TargetType="ToggleButton">
    <Setter Property="Height" Value="32" />
    <Setter Property="Margin" Value="0,0,0,2" />
    <!-- Stretch, or the default Center shrink-wraps the row and the star column
         - and with it the name - collapses to nothing. -->
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="Foreground" Value="{DynamicResource SideNav.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Grid>
            <Border x:Name="Fill" Background="{TemplateBinding Background}"
                    CornerRadius="{StaticResource Radius.Control}" />
            <!-- Rounded on its left pair only, or a square 2 DIP strip hangs off
                 the corners of a 3 DIP pill. A Border does not clip its child to
                 a corner radius, so nesting the rail inside the fill does not
                 solve it. -->
            <Border x:Name="Indicator" Width="2" HorizontalAlignment="Left"
                    CornerRadius="3,0,0,3" Background="Transparent" />
            <ContentPresenter Margin="7,0" VerticalAlignment="Center"
                              HorizontalAlignment="Stretch" />
          </Grid>
          <ControlTemplate.Triggers>
            <!-- Guarded on IsEnabled, because WPF raises IsMouseOver on a
                 disabled element too - that is what makes ShowOnDisabled work -
                 and a greyed section that lights up reads as clickable and then
                 refuses the click. The foreground moves as well as the fill: in
                 the light palette the hover wash is two per cent off the nav's
                 own background, so on its own it says almost nothing. -->
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsMouseOver" Value="True" />
                <Condition Property="IsEnabled" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Fill" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
              <Setter Property="Foreground"
                      Value="{DynamicResource SideNav.ActiveForeground}" />
            </MultiTrigger>
            <!-- After the hover trigger, so hovering the selected item does not
                 replace its selection fill with the hover one. -->
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Fill" Property="Background"
                      Value="{DynamicResource List.ActiveSelection}" />
              <Setter TargetName="Indicator" Property="Background"
                      Value="{DynamicResource SideNav.ActiveBorder}" />
              <Setter Property="Foreground"
                      Value="{DynamicResource SideNav.ActiveForeground}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Indicator" Property="Background"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
            <!-- Last, so the disabled colour beats selection and hover both. -->
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
              <Setter Property="Cursor" Value="Arrow" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- E76B and E76C are ChevronLeft and ChevronRight in Segoe MDL2 Assets, the
       same face the section glyphs use, so nothing else in the nav is redrawn.
       The tooltip names the act rather than the state, so a screen reader hears
       what clicking will do.

       Radius.Control rather than the handoff's 2: this file carries a standing
       rule against a seventh radius, and 3 on a 26 DIP square is not tellable
       from 2. -->
  <Style x:Key="NavigationChevron" TargetType="Button">
    <Setter Property="Width" Value="26" />
    <Setter Property="Height" Value="26" />
    <Setter Property="Foreground" Value="{DynamicResource SideNav.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Content" Value="&#xE76B;" />
    <Setter Property="ToolTip" Value="Hide the section names" />
    <Setter Property="AutomationProperties.Name" Value="Hide the section names" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Fill" Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius.Control}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Fill" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
              <Setter Property="Foreground"
                      Value="{DynamicResource SideNav.ActiveForeground}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Fill" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
              <Setter Property="Foreground"
                      Value="{DynamicResource SideNav.ActiveForeground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsNavCollapsed}" Value="True">
        <Setter Property="Content" Value="&#xE76C;" />
        <Setter Property="ToolTip" Value="Show the section names" />
        <Setter Property="AutomationProperties.Name" Value="Show the section names" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="ListItem" TargetType="ListBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource SideBar.Foreground}" />
    <Setter Property="Padding" Value="16,4" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Border x:Name="Root" Background="Transparent" Padding="{TemplateBinding Padding}"
                  CornerRadius="{StaticResource Radius.Control}">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.ActiveSelection}" />
              <Setter Property="Foreground" Value="{DynamicResource List.ActiveSelectionText}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
              <Setter TargetName="Root" Property="BorderThickness" Value="1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PrimaryButton" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource Button.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Button.Foreground}" />

    <!-- Its own states, carried on the button, so the one template below can
         serve a style whose states are different without a second copy of it. -->
    <Setter Property="theme:ButtonStates.HoverBackground"
            Value="{DynamicResource Button.HoverBackground}" />
    <Setter Property="theme:ButtonStates.PressedBackground"
            Value="{DynamicResource Button.PressedBackground}" />

    <!-- The accent button's outline is its own fill, as the theme specifies, so
         it reads as a solid block rather than a bordered one - but the border is
         drawn all the same, because a button that grows one only when focused
         moves its label a pixel as it arrives. -->
    <Setter Property="BorderBrush" Value="{DynamicResource Button.Background}" />
    <Setter Property="theme:ButtonStates.HoverBorder"
            Value="{DynamicResource Button.HoverBackground}" />
    <Setter Property="BorderThickness" Value="1" />

    <Setter Property="FontSize" Value="13" />
    <Setter Property="Padding" Value="15,6" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Padding="{TemplateBinding Padding}"
                  CornerRadius="{StaticResource Radius.Control}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <!-- Read off the button rather than named here, so a style that
                 changes its Background changes its hover with it. -->
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{Binding RelativeSource={RelativeSource TemplatedParent},
                                      Path=(theme:ButtonStates.HoverBackground)}" />
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{Binding RelativeSource={RelativeSource TemplatedParent},
                                      Path=(theme:ButtonStates.HoverBorder)}" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{Binding RelativeSource={RelativeSource TemplatedParent},
                                      Path=(theme:ButtonStates.PressedBackground)}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Root" Property="Opacity" Value="0.5" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
    <Setter Property="Background" Value="{DynamicResource Button.SecondaryBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource SideBar.Foreground}" />

    <!-- Its own hover and pressed, which is the whole point of the two
         properties: without them this inherited the primary button's accent and
         a quiet grey button lit up teal under the pointer. -->
    <Setter Property="theme:ButtonStates.HoverBackground"
            Value="{DynamicResource Button.SecondaryHover}" />
    <Setter Property="theme:ButtonStates.PressedBackground"
            Value="{DynamicResource Button.SecondaryPressedBackground}" />

    <!-- The outline the theme has always specified for a standard button, and
         which this style did not draw. Its fill is ControlBackground and so is
         the surface most of these sit on, so without the border the button had
         no edge at all until the pointer was over it - invisible on the About
         card, and nearly so beside a text box. -->
    <Setter Property="BorderBrush" Value="{DynamicResource Input.Border}" />
    <Setter Property="theme:ButtonStates.HoverBorder"
            Value="{DynamicResource ControlBorderHover}" />
  </Style>

  <!--
    One segment of a two-way picker. Still a RadioButton rather than a
    ToggleButton, so the pair stays mutually exclusive on its own, keeps arrow-key
    navigation, and reads to a screen reader as one choice rather than two
    unrelated switches - only the dot and label are replaced.

    The checked trigger sits after the hover one deliberately: later triggers win,
    so the selected segment keeps its accent instead of greying under the pointer.
  -->
  <Style x:Key="SegmentedToggle" TargetType="RadioButton">
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Padding" Value="14,5" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  Padding="{TemplateBinding Padding}"
                  CornerRadius="{StaticResource Radius.Inner}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource Button.SecondaryHover}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource Button.Background}" />
              <Setter Property="Foreground" Value="{DynamicResource Button.Foreground}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
              <Setter TargetName="Root" Property="BorderThickness" Value="1" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!--
    A tab. A RadioButton like SegmentedToggle, and deliberately nothing like one
    to look at: the segmented control fills the chosen segment and floats free,
    whereas a tab sits on a rule that runs the width of the content and marks
    itself by colouring its own piece of that rule.

    The difference is what says these are two views of one screen rather than two
    buttons that happen to be next to each other - which is the whole reason to
    have tabs instead of a longer page.
  -->
  <!--
    A link inside a sentence. Implicit, because there is only one kind of them
    and naming a key at every use would be noise.

    Not LinkButton: that is a Button, so it cannot sit inside a paragraph - it
    breaks the line and drops the address onto one of its own, which is exactly
    what the credits were doing before. A Hyperlink is a text element and flows
    with the words either side of it.

    Colour at rest and an underline on hover, which is what LinkButton does, so
    the two read as the same thing in different places.
  -->
  <Style TargetType="Hyperlink">
    <Setter Property="Foreground" Value="{DynamicResource Link.Foreground}" />
    <Setter Property="TextDecorations" Value="{x:Null}" />
    <Setter Property="Cursor" Value="Hand" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="TextDecorations" Value="Underline" />
      </Trigger>
      <Trigger Property="IsKeyboardFocused" Value="True">
        <Setter Property="TextDecorations" Value="Underline" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="TabToggle" TargetType="RadioButton">
    <!-- No dotted rectangle. Selection and keyboard focus are the same thing in a
         radio group, and the accent underline already says which tab that is, so
         the default visual only adds a second marker around the first. -->
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Placeholder}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="Padding" Value="2,7" />
    <Setter Property="Margin" Value="0,0,22,0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <!-- The underline is drawn transparent at rest rather than added on
               selection, so choosing a tab recolours a line that is already
               there and the label does not shift a pixel upwards as it appears.
               The same reason LinkButton keeps its focus ring transparent. -->
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  Padding="{TemplateBinding Padding}"
                  BorderBrush="Transparent" BorderThickness="0,0,0,2"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Panel.Border}" />
            </Trigger>
            <!-- After the hover trigger: later triggers win, so the chosen tab
                 keeps its accent under the pointer instead of greying to the
                 hover line. -->
            <Trigger Property="IsChecked" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource AccentBackground}" />
            </Trigger>
            <!-- No box on focus. WPF gives a RadioButton keyboard focus when it
                 is clicked as well as when it is arrowed to, so a filled
                 background here appeared on every click and made the tab read as
                 a button. Arrow keys move the selection with the focus in a
                 radio group, so the accent underline is already saying where the
                 keyboard is. -->
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="LinkButton" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource Link.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Padding" Value="0,2" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <!-- The ring is drawn transparent at rest rather than added on focus,
               so arriving by keyboard colours a border that is already there and
               the text does not shift a pixel sideways as it appears. -->
          <Border x:Name="Root" Background="Transparent" Margin="-1,0"
                  BorderBrush="Transparent" BorderThickness="1"
                  CornerRadius="{StaticResource Radius.Inner}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Left" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="TextBlock.TextDecorations" Value="Underline" />
            </Trigger>
            <!-- A link has no fill and no outline to change, so without this it
                 was the one control in the app a keyboard could reach and not
                 show it had reached. -->
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter Property="TextBlock.TextDecorations" Value="Underline" />
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The app's first progress bar. Stock WPF chrome is Aero-flavoured and would
       not sit beside these flat buttons, so it is templated like everything else
       here. PART_Track and PART_Indicator are both required: ProgressBar sizes the
       indicator against the track and draws nothing if either is missing. The
       colours are the existing neutral ramp and the same blue as the status text
       beside it, so no new theme keys are needed. -->
  <Style x:Key="PassProgress" TargetType="ProgressBar">
    <Setter Property="Height" Value="6" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Background" Value="{DynamicResource Neutral.3}" />
    <Setter Property="Foreground" Value="{DynamicResource Status.Info}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ProgressBar">
          <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True"
                  ClipToBounds="True">
            <Grid>
              <Rectangle x:Name="PART_Track" />
              <Rectangle x:Name="PART_Indicator" HorizontalAlignment="Left"
                         Fill="{TemplateBinding Foreground}" />
              <!-- Shown only while there is no total to fill towards, which is the
                   whole of a crawl: it cannot know how many files it will find
                   until it has found them. -->
              <Rectangle x:Name="Runner" Width="90" Visibility="Collapsed"
                         HorizontalAlignment="Left" Fill="{TemplateBinding Foreground}">
                <Rectangle.RenderTransform>
                  <TranslateTransform x:Name="RunnerShift" />
                </Rectangle.RenderTransform>
              </Rectangle>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsIndeterminate" Value="True">
              <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
              <Setter TargetName="Runner" Property="Visibility" Value="Visible" />
              <Trigger.EnterActions>
                <BeginStoryboard x:Name="Sliding">
                  <!-- Travels further than any bar here is wide and is clipped, so
                       it leaves cleanly whatever the card's width turns out to be. -->
                  <Storyboard RepeatBehavior="Forever">
                    <DoubleAnimation Storyboard.TargetName="RunnerShift"
                                     Storyboard.TargetProperty="X"
                                     From="-90" To="560" Duration="0:0:1.4" />
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Trigger.ExitActions>
                <StopStoryboard BeginStoryboardName="Sliding" />
              </Trigger.ExitActions>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Set-up screen typography -->
  <Style x:Key="Eyebrow" TargetType="TextBlock">
    <Setter Property="FontSize" Value="11" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Placeholder}" />
    <Setter Property="Margin" Value="0,0,0,10" />
  </Style>

  <Style x:Key="Heading" TargetType="TextBlock">
    <Setter Property="FontSize" Value="28" />
    <Setter Property="FontWeight" Value="Bold" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="Margin" Value="0,0,0,12" />
  </Style>

  <Style x:Key="BodyText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="13.5" />
    <Setter Property="LineHeight" Value="21" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Placeholder}" />
  </Style>

  <Style x:Key="FieldLabel" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="Margin" Value="0,0,0,6" />
  </Style>

  <Style x:Key="HintText" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Placeholder}" />
    <Setter Property="Margin" Value="0,7,0,0" />
  </Style>

  <!-- Where one model file stands, said in colour as well as in words. The list
       is read while a download is running, and rereading six lines to find the
       one that has not arrived is work the colour does instead. Green for here,
       red for a file that was not what it claimed to be, and the ordinary muted
       grey for one still to come - so only the two states worth acting on carry
       any colour at all. -->
  <Style x:Key="ModelFileState" TargetType="TextBlock" BasedOn="{StaticResource HintText}">
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsReady}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource Status.Success}" />
      </DataTrigger>

      <!-- Last, so it wins: a damaged file is the one thing here that needs
           doing something about. -->
      <DataTrigger Binding="{Binding WasDamaged}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource Status.Danger}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- The tint behind that state, so it reads as a badge rather than as a word
       in a sentence. Every state gets one, including the quiet grey of a file
       still to come: a pill on some rows and bare text on others makes a column
       that looks broken rather than one that reads at a glance.

       Every brush here is defined in both palettes, and named through
       DynamicResource, so the whole thing swaps with the theme. -->
  <Style x:Key="StatePill" TargetType="Border">
    <Setter Property="CornerRadius" Value="{StaticResource Radius.Inner}" />
    <Setter Property="Padding" Value="7,2" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Background" Value="{DynamicResource Badge.Background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Panel.Border}" />
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsReady}" Value="True">
        <Setter Property="Background" Value="{DynamicResource Status.SuccessBack}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Status.Success}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding WasDamaged}" Value="True">
        <Setter Property="Background" Value="{DynamicResource Status.DangerBack}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Status.Danger}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- The same badge for a whole feature, which has one state more: half
       installed. -->
  <Style x:Key="FeaturePill" TargetType="Border" BasedOn="{StaticResource StatePill}">
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsPartial}" Value="True">
        <Setter Property="Background" Value="{DynamicResource Status.CautionBack}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Status.Caution}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsReady}" Value="True">
        <Setter Property="Background" Value="{DynamicResource Status.SuccessBack}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Status.Success}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding WasDamaged}" Value="True">
        <Setter Property="Background" Value="{DynamicResource Status.DangerBack}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Status.Danger}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- And where a whole feature stands, in the corner of its card. Amber for
       half-finished, because that is the state a user leaves by mistake -
       "1 of 2 files" in the same grey as everything else reads like a note
       rather than something unfinished. -->
  <Style x:Key="FeatureState" TargetType="TextBlock" BasedOn="{StaticResource HintText}">
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsPartial}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource Status.Caution}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsReady}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource Status.Success}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding WasDamaged}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource Status.Danger}" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <Style x:Key="InputTextBox" TargetType="TextBox">
    <Setter Property="Background" Value="{DynamicResource Input.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Input.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Input.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="10,7" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius.Control}"
                  SnapsToDevicePixels="True">
            <ScrollViewer x:Name="PART_ContentHost"
                          Margin="{TemplateBinding Padding}"
                          VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
              <Setter TargetName="Root" Property="BorderThickness" Value="2" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="StatusBarText" TargetType="TextBlock">
    <Setter Property="Foreground" Value="{DynamicResource StatusBar.Foreground}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Margin" Value="0,0,16,0" />
  </Style>

  <!-- Gallery. A row of pictures is the list's item, so the stock virtualising
       stack panel can recycle rows; a wrap panel of 11,482 pictures realises
       every one of them and costs 90 MB of visual tree. Every row must be the
       same height, so the sizes come from GalleryLayout rather than being
       repeated here. -->
  <Style x:Key="GalleryRowItem" TargetType="ListBoxItem">
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <ContentPresenter />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="GalleryList" TargetType="ListBox">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource GalleryRowItem}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    <!-- Recycling reuses containers instead of rebuilding them; pixel scrolling
         stops the wheel jumping a whole row at a time. The cache length already
         defaults to one viewport either side, so it is left alone. -->
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
    <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
  </Style>

  <!--
    One album on the albums wall: a cover, a name and what is in it. The
    whole card is the button - half a card that opens and half that does not is
    a coin toss - and the picture is clipped to the card's own corners so a
    landscape cover does not square off a rounded tile.
  -->
  <!--
    A row that is on or off: a tick box and a label, in a list. The app had no
    checkbox anywhere until an album needed a rule built out of people and
    places, and one drawn from the same glyph set as the rest of the app is
    cheaper than teaching the Windows default about the theme.
  -->
  <Style x:Key="CheckRow" TargetType="ToggleButton">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius.Inner}" Padding="6,4"
                  SnapsToDevicePixels="True">
            <StackPanel Orientation="Horizontal">
              <TextBlock x:Name="Tick" Text="&#xE739;" FontFamily="Segoe MDL2 Assets"
                         FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"
                         Foreground="{DynamicResource Editor.Placeholder}" />
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Tick" Property="Text" Value="&#xE73A;" />
              <Setter TargetName="Tick" Property="Foreground"
                      Value="{DynamicResource AccentBackground}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="AlbumCard" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Margin" Value="0,0,16,18" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  BorderBrush="Transparent" BorderThickness="1"
                  CornerRadius="{StaticResource Radius.Control}" Padding="6"
                  SnapsToDevicePixels="True">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="GalleryCell" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource Neutral.3}" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  SnapsToDevicePixels="True">
            <Grid ClipToBounds="True">
              <!-- Drawn under the picture, so a cell that has nothing to show
                   yet keeps the grid's geometry instead of leaving a hole. -->
              <TextBlock Text="&#xEB9F;" FontFamily="Segoe MDL2 Assets" FontSize="26"
                         HorizontalAlignment="Center" VerticalAlignment="Center"
                         Foreground="{DynamicResource Editor.Placeholder}" />
              <ContentPresenter />
              <Border x:Name="Focus" BorderBrush="{DynamicResource Focus.Border}"
                      BorderThickness="0" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Focus" Property="BorderThickness" Value="2" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Focus" Property="BorderThickness" Value="2" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="VideoBadge" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource Badge.Background}" />
    <Setter Property="HorizontalAlignment" Value="Right" />
    <Setter Property="VerticalAlignment" Value="Bottom" />
    <Setter Property="Margin" Value="0,0,6,6" />
    <Setter Property="Padding" Value="5,2" />
  </Style>

  <Style x:Key="ViewerButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
    <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
    <Setter Property="FontSize" Value="16" />
    <Setter Property="Padding" Value="14,10" />
    <Setter Property="MinWidth" Value="48" />
  </Style>

  <!-- Folder tree. Stock TreeView draws its own selection in system colours,
       which reads as a foreign control inside this shell, and its expander is a
       hairline triangle. Both are replaced. -->
  <Style x:Key="FolderExpander" TargetType="ToggleButton">
    <Setter Property="Width" Value="16" />
    <Setter Property="Height" Value="16" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Placeholder}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border Background="Transparent">
            <TextBlock x:Name="Glyph" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets"
                       FontSize="9" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{TemplateBinding Foreground}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Glyph" Property="Text" Value="&#xE70D;" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Glyph" Property="Foreground"
                      Value="{DynamicResource Editor.Foreground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="FolderTreeItem" TargetType="TreeViewItem">
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="IsExpanded" Value="False" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TreeViewItem">
          <StackPanel>
            <!-- A hairline under each row, so a dense tree stays readable. -->
            <Border x:Name="Row" Background="Transparent" Padding="0,5"
                    BorderBrush="{DynamicResource Separator}" BorderThickness="0,0,0,1">
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="18" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ToggleButton Grid.Column="0" Style="{DynamicResource FolderExpander}"
                              ClickMode="Press"
                              IsChecked="{Binding IsExpanded,
                                          RelativeSource={RelativeSource TemplatedParent}}"
                              Visibility="{TemplateBinding HasItems,
                                           Converter={StaticResource BoolToVisibility}}" />
                <ContentPresenter Grid.Column="1" ContentSource="Header"
                                  VerticalAlignment="Center" />
              </Grid>
            </Border>
            <ItemsPresenter x:Name="Children" Margin="14,0,0,0" />
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="False">
              <Setter TargetName="Children" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Row" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Row" Property="Background"
                      Value="{DynamicResource List.ActiveSelection}" />
              <Setter Property="Foreground"
                      Value="{DynamicResource List.ActiveSelectionText}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="FolderTree" TargetType="TreeView">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="{DynamicResource Editor.Foreground}" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource FolderTreeItem}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
  </Style>

  <!-- One name offered by the search box. A Button rather than a list item
       because picking it is an action, not a selection that persists. -->
  <Style x:Key="SearchResult" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Foreground" Value="{DynamicResource SideBar.Foreground}" />
    <Setter Property="Padding" Value="16,7" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Face" Background="{TemplateBinding Background}"
                  Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Stretch" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Face" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Face" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- An on/off switch, the shape a phone uses. A button labelled with the
       action it performs has to be read before its state is known; a switch
       shows the state and needs no label at all. Neutral.6 reads as "off" in
       both palettes, and the accent as "on". -->
  <Style x:Key="Switch" TargetType="ToggleButton">
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Width" Value="38" />
    <Setter Property="Height" Value="22" />
    <Setter Property="HorizontalAlignment" Value="Left" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Track" CornerRadius="11" SnapsToDevicePixels="True"
                  Background="{DynamicResource Neutral.6}">
            <Border x:Name="Knob" Width="16" Height="16" CornerRadius="8" Margin="3,0,0,0"
                    HorizontalAlignment="Left" VerticalAlignment="Center"
                    Background="{DynamicResource Button.Foreground}" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Track" Property="Background"
                      Value="{DynamicResource Button.Background}" />
              <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right" />
              <Setter TargetName="Knob" Property="Margin" Value="0,0,3,0" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Track" Property="Opacity" Value="0.85" />
            </Trigger>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Track" Property="BorderThickness" Value="2" />
              <Setter TargetName="Track" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- A standalone on/off button. Not SegmentedToggle, which is a RadioButton
       style and belongs to a group of mutually exclusive choices - applying it
       to a ToggleButton throws while the window is being parsed, which takes the
       whole app down before it has drawn anything. -->
  <Style x:Key="PillToggle" TargetType="ToggleButton">
    <Setter Property="Background" Value="{DynamicResource Button.SecondaryBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource SideBar.Foreground}" />
    <Setter Property="Padding" Value="14,7" />
    <Setter Property="FontSize" Value="12.5" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Face" Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Radius.Control}"
                  Padding="{TemplateBinding Padding}" SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Face" Property="Background"
                      Value="{DynamicResource Button.Background}" />
              <Setter Property="Foreground" Value="{DynamicResource Button.Foreground}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Face" Property="Opacity" Value="0.9" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- A face outlined on the open photograph. Green once somebody has said who
       it is, so a picture shows at a glance what is left to do. -->
  <Style x:Key="FaceBox" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Outline" BorderThickness="2" Background="Transparent"
                  BorderBrush="{DynamicResource Status.Caution}"
                  CornerRadius="{StaticResource Radius.Inner}" SnapsToDevicePixels="True">
            <ContentPresenter VerticalAlignment="Bottom" HorizontalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding IsNamed}" Value="True">
              <Setter TargetName="Outline" Property="BorderBrush"
                      Value="{DynamicResource Status.Success}" />
            </DataTrigger>
            <!-- Set aside: still outlined, so it can be brought back, but faded
                 out of the way of the faces that still need an answer. -->
            <DataTrigger Binding="{Binding IsIgnored}" Value="True">
              <Setter TargetName="Outline" Property="BorderBrush"
                      Value="{DynamicResource Neutral.6}" />
              <Setter TargetName="Outline" Property="Opacity" Value="0.45" />
            </DataTrigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Outline" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="FaceBoxLabel" TargetType="Border">
    <Setter Property="Background" Value="#CC1E1E1E" />
    <Setter Property="Padding" Value="5,1" />
    <Setter Property="CornerRadius" Value="{StaticResource Radius.Inner}" />
    <Setter Property="Margin" Value="0,0,0,3" />
  </Style>

  <!-- People -->

  <!-- One question on the review list. A panel rather than a table row: it holds
       a grid of faces and the controls that answer it, which no row layout in
       this app is shaped for. -->
  <Style x:Key="PeopleCard" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SideBar.Background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Panel.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource Radius.Control}" />
    <Setter Property="Padding" Value="14,12" />
    <Setter Property="Margin" Value="0,0,0,10" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
  </Style>

  <Style x:Key="FaceCrop" TargetType="Image">
    <Setter Property="Width" Value="100" />
    <Setter Property="Height" Value="100" />
    <Setter Property="Stretch" Value="UniformToFill" />
    <Setter Property="Margin" Value="0,0,6,6" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
  </Style>

  <!-- The way out of a crop and into the picture it came from. Drawn on the
       crop rather than beside it: the question is about that one face, and a
       row of buttons under a wall of faces belongs to none of them. -->
  <Style x:Key="CropAction" TargetType="Button">
    <Setter Property="Width" Value="22" />
    <Setter Property="Height" Value="22" />
    <Setter Property="Margin" Value="0,4,10,0" />
    <Setter Property="HorizontalAlignment" Value="Right" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Opacity" Value="0.6" />
    <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="#CC1E1E1E" CornerRadius="11" SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Opacity" Value="1" />
      </Trigger>
    </Style.Triggers>
  </Style>

  <!-- The face being asked about, outlined on the whole picture. Not hit
       testable: the answer is given by the buttons, and a box that swallowed
       clicks would only be a way to miss them. -->
  <Style x:Key="ProposalOutline" TargetType="Border">
    <Setter Property="BorderThickness" Value="2" />
    <Setter Property="BorderBrush" Value="{DynamicResource Status.Caution}" />
    <Setter Property="CornerRadius" Value="{StaticResource Radius.Inner}" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
    <Setter Property="IsHitTestVisible" Value="False" />
  </Style>

  <!-- A name in the "who is this?" list. The one already on the face is marked
       in the name itself as well as by the tick beside it: a tick in the far
       corner of a list of nine is easy to miss, and the thing being changed is
       the name. -->
  <Style x:Key="FaceChoiceName" TargetType="TextBlock">
    <Style.Triggers>
      <DataTrigger Binding="{Binding IsCurrent}" Value="True">
        <Setter Property="Foreground" Value="{DynamicResource Status.Success}" />
        <Setter Property="FontWeight" Value="SemiBold" />
      </DataTrigger>
    </Style.Triggers>
  </Style>

  <!-- A proposed face, which the user is answering yes or no to. Chosen is the
       default, so a whole screenful is confirmed with one click and the odd
       wrong one is switched off first. -->
  <Style x:Key="FaceChoice" TargetType="ToggleButton">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Margin" Value="0,0,6,6" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ToggleButton">
          <Border x:Name="Frame" Padding="3" CornerRadius="{StaticResource Radius.Control}"
                  Background="Transparent" BorderThickness="2" BorderBrush="Transparent"
                  SnapsToDevicePixels="True">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Frame" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
            </Trigger>
            <Trigger Property="IsChecked" Value="False">
              <Setter TargetName="Frame" Property="Opacity" Value="0.35" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Frame" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!--
    What is known about one picture. One template for the photo viewer, the face
    review and the duplicate comparison, so the same photograph reads the same
    way whichever screen you reached it from.

    The file name opens Explorer on the original; the app only ever reads its own
    small copies, so this is the way to the real thing.
  -->
  <DataTemplate DataType="{x:Type shell:PhotoDetails}">
    <StackPanel>
      <TextBlock Style="{DynamicResource FieldLabel}" Text="File" />
      <Button Style="{DynamicResource LinkButton}" FontSize="13" HorizontalAlignment="Left"
              Content="{Binding FileName}" ToolTip="{Binding FullPath}"
              Command="{Binding ShowInExplorerCommand}" />
      <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                 Text="{Binding FolderPath}" />

      <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="Size" />
      <TextBlock Style="{DynamicResource HintText}" Text="{Binding ExactSize}" />

      <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="Resolution" />
      <TextBlock Style="{DynamicResource HintText}" Text="{Binding Resolution}" />

      <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="Taken" />
      <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap" Text="{Binding When}" />

      <!-- Both file dates, plainly, and which of the three the app files this
           picture under. The order matters on this library: copying stamps the
           creation date with the day of the copy and leaves the modified date
           alone, so the created date is usually the later and usually the
           wrong one. -->
      <StackPanel Visibility="{Binding HasFileDates,
                               Converter={StaticResource BoolToVisibility}}">
        <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="File created" />
        <TextBlock Style="{DynamicResource HintText}" Text="{Binding Created}" />

        <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="File modified" />
        <TextBlock Style="{DynamicResource HintText}" Text="{Binding Modified}" />

        <!-- Only when the filing date is not the one on show above. Collapsed
             rather than blank, so the line takes no room when it has nothing to
             say. -->
        <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                   Text="{Binding FiledUnder}"
                   Visibility="{Binding FiledUnder,
                                Converter={StaticResource InverseNullToVisibility}}" />
      </StackPanel>

      <!-- Absent, not blank. Five photographs in six carry no GPS and never
           will, so a label with nothing under it would read as something the app
           failed at rather than something the camera never recorded. -->
      <StackPanel Visibility="{Binding PlaceName,
                               Converter={StaticResource InverseNullToVisibility}}">
        <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="Place" />
        <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                   Text="{Binding PlaceName}" />
      </StackPanel>

      <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0"
                 Text="Fingerprint (SHA-256)" />
      <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                 FontFamily="{StaticResource Font.Mono}" FontSize="11"
                 Text="{Binding Fingerprint}" />
    </StackPanel>
  </DataTemplate>

  <!--
    Saying which album a photograph belongs in. The same shape as the name
    picker below, because it is the same question in a different subject: one box
    that narrows the list and, when nothing is left, becomes the new name.
  -->
  <DataTemplate DataType="{x:Type albums:AlbumPicker}">
    <!-- ModalSurface, like every other thing that floats over the app. Only the
         padding is its own: a dialog's 24 around a panel this narrow reads as a
         box with a small question lost in the middle of it. -->
    <Border Style="{DynamicResource ModalSurface}" Padding="16,14"
            MaxWidth="340" HorizontalAlignment="Center" VerticalAlignment="Center">
      <StackPanel>
        <TextBlock Style="{DynamicResource FieldLabel}" Text="{Binding Prompt}" />
        <TextBlock Style="{DynamicResource HintText}" Margin="0,2,0,8"
                   TextWrapping="Wrap" Text="{Binding Hint}" />

        <!-- Above the list rather than under it. Under a list capped at 300 DIP
             this box is below the fold the moment there are more albums than fit,
             and sitting beside "Make" it read as the way to create one rather
             than the way to find one - which is how a picker that has always
             filtered came to be reported as having no search. -->
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBox Grid.Column="0" Style="{DynamicResource InputTextBox}"
                   AutomationProperties.Name="Search albums, or type a new name"
                   ToolTip="Type to find an album, or type a name that is not there yet"
                   Text="{Binding Typed, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.InputBindings>
              <KeyBinding Key="Return" Command="{Binding AddCommand}" />
            </TextBox.InputBindings>
          </TextBox>
          <!-- Beside the box whether or not anything matches: an album can want a
               name that is a prefix of one already there, and hiding this until
               nothing matched would make that name impossible to give. -->
          <Button Grid.Column="1" Style="{DynamicResource SecondaryButton}"
                  Margin="6,0,0,0" MinWidth="52" Content="Make"
                  ToolTip="Make an album with this name and put the photograph in it"
                  Command="{Binding AddCommand}" />
        </Grid>

        <!-- A filtered list and a short list look the same. -->
        <TextBlock Style="{DynamicResource HintText}" Margin="0,6,0,2"
                   Text="{Binding Narrowed}"
                   Visibility="{Binding IsNarrowed,
                                Converter={StaticResource BoolToVisibility}}" />

        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding Choices}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Style="{DynamicResource SearchResult}" Padding="10,6"
                        Command="{Binding DataContext.ChooseCommand,
                                  RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding}">
                  <Grid>
                    <StackPanel HorizontalAlignment="Left">
                      <TextBlock Text="{Binding Name}"
                                 Style="{DynamicResource FaceChoiceName}" />
                      <TextBlock Text="{Binding Caption}" FontSize="11"
                                 Foreground="{DynamicResource Editor.Placeholder}" />
                    </StackPanel>
                    <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                               FontSize="11" HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Status.Success}"
                               Visibility="{Binding IsCurrent,
                                            Converter={StaticResource BoolToVisibility}}" />
                  </Grid>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>

        <!-- The other answer to which album this is in, which is none of them.
             Here rather than on the strip beside the picture: it is the same
             question, and out there it was a second icon button wearing a tick.
             Named after the album so it cannot be pressed by mistake, and only
             offered when there is one to leave. -->
        <Button Style="{DynamicResource SecondaryButton}" Margin="0,10,0,0"
                HorizontalAlignment="Stretch"
                Content="{Binding TakeOutLabel}"
                ToolTip="Leave it in no album at all. The photograph itself is untouched."
                Visibility="{Binding IsInOne, Converter={StaticResource BoolToVisibility}}"
                Command="{Binding TakeOutCommand}" />

        <Button Style="{DynamicResource LinkButton}" Margin="0,10,0,0"
                HorizontalAlignment="Center" Content="Cancel"
                Command="{Binding CancelCommand}" />
      </StackPanel>
    </Border>
  </DataTemplate>

  <!--
    Saying who a face is. One template, wherever the question is asked: over a
    photograph in the viewer, and over the picture being reviewed on the People
    screen. The wording and whether "nobody" is on offer come from the picker
    itself, so the two screens cannot drift apart.
  -->
  <DataTemplate DataType="{x:Type people:PersonPicker}">
    <!-- The same surface and the same narrower padding as the album
         picker above, for the same reasons. -->
    <Border Style="{DynamicResource ModalSurface}" Padding="16,14"
            MaxWidth="320" HorizontalAlignment="Center" VerticalAlignment="Center">
      <StackPanel>
        <TextBlock Style="{DynamicResource FieldLabel}" Text="{Binding Prompt}" />
        <TextBlock Style="{DynamicResource HintText}" Margin="0,2,0,10"
                   TextWrapping="Wrap" Text="{Binding Hint}" />

        <!-- Scrolls rather than growing without limit: the panel is centred on
             the picture, and a list of thirty names would otherwise run off
             both ends of the window. -->
        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding Choices}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button Style="{DynamicResource SearchResult}" Padding="10,6"
                        Command="{Binding DataContext.ChooseCommand,
                                  RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                        CommandParameter="{Binding}">
                  <Grid>
                    <!-- The name already on this face, so changing it is a
                         change the user can see they are making. -->
                    <TextBlock Text="{Binding DisplayName}" HorizontalAlignment="Left"
                               Style="{DynamicResource FaceChoiceName}" />
                    <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets"
                               FontSize="11" HorizontalAlignment="Right"
                               Foreground="{DynamicResource Status.Success}"
                               Visibility="{Binding IsCurrent,
                                            Converter={StaticResource BoolToVisibility}}" />
                  </Grid>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>

        <TextBlock Style="{DynamicResource HintText}" Margin="0,8,0,4"
                   Text="Type to narrow the list, or to add somebody new" />
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBox Grid.Column="0" Style="{DynamicResource InputTextBox}"
                   AutomationProperties.Name="Find or add a person"
                   Text="{Binding Typed, UpdateSourceTrigger=PropertyChanged}">
            <TextBox.InputBindings>
              <KeyBinding Key="Return" Command="{Binding AddCommand}" />
            </TextBox.InputBindings>
          </TextBox>
          <Button Grid.Column="1" Style="{DynamicResource SecondaryButton}"
                  Margin="8,0,0,0" MinWidth="60" Content="Add"
                  Command="{Binding AddCommand}" />
        </Grid>

        <TextBlock Style="{DynamicResource HintText}" Margin="0,6,0,0" TextWrapping="Wrap"
                   Text="Nobody by that name yet. Add makes them."
                   Visibility="{Binding HasNoMatch,
                                Converter={StaticResource BoolToVisibility}}" />

        <!-- Strangers in the background outnumber the people a library is
             about, and refusing them one person at a time never ends. Offered
             only where the screen has somewhere to put such a face. -->
        <Button Style="{DynamicResource SecondaryButton}" Margin="0,12,0,0"
                HorizontalAlignment="Stretch" Content="{Binding SetAsideLabel}"
                ToolTip="Set this face aside. Naming it later brings it back."
                Command="{Binding SetAsideCommand}"
                Visibility="{Binding CanSetAside,
                             Converter={StaticResource BoolToVisibility}}" />

        <Button Style="{DynamicResource LinkButton}" Margin="0,10,0,0"
                HorizontalAlignment="Left" Content="Cancel"
                Command="{Binding CancelCommand}" />
      </StackPanel>
    </Border>
  </DataTemplate>

  <!--
    A date picker, and the calendar it drops down.

    Written out rather than left stock because WPF draws both from the Aero
    theme, which is a white box with blue Windows-7 gradients: on this app's dark
    surface it is the one control that announces it was not designed here. The
    templates below are the stock part structure - the PART_ names are what the
    controls find their pieces by - repainted from the same brushes as
    everything else.

    The typed box is kept as well as the calendar. A date somebody already knows
    is faster typed than clicked back through, and the text box is the half of
    the control that a keyboard can reach.
  -->
  <Style x:Key="CalendarNavButton" TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
    <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
    <Setter Property="FontSize" Value="11" />
    <Setter Property="Width" Value="28" />
    <Setter Property="Height" Value="28" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Root" Background="Transparent" CornerRadius="4">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The month and year cells behind the header button. -->
  <Style x:Key="CalendarMonthButton" TargetType="CalendarButton">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="MinWidth" Value="48" />
    <Setter Property="MinHeight" Value="40" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CalendarButton">
          <Border x:Name="Root" Background="Transparent" Margin="1" CornerRadius="4">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="HasSelectedDays" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource AccentBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource AccentForeground}" />
            </Trigger>
            <Trigger Property="IsInactive" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CalendarDay" TargetType="CalendarDayButton">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
    <Setter Property="FontSize" Value="12" />
    <Setter Property="MinWidth" Value="32" />
    <Setter Property="MinHeight" Value="30" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CalendarDayButton">
          <Border x:Name="Root" Background="Transparent" Margin="1" CornerRadius="4"
                  BorderThickness="1" BorderBrush="Transparent">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <!-- Ordered least to most specific: selection has to win over both
                 today's ring and the hover wash, and blacked-out over all of
                 them. -->
            <Trigger Property="IsInactive" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
            </Trigger>
            <Trigger Property="IsToday" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource AccentBackground}" />
              <Setter Property="FontWeight" Value="SemiBold" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource List.HoverBackground}" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource AccentBackground}" />
              <Setter Property="Foreground" Value="{DynamicResource AccentForeground}" />
            </Trigger>
            <Trigger Property="IsBlackedOut" Value="True">
              <Setter Property="Foreground" Value="{DynamicResource TextDisabled}" />
              <Setter Property="IsEnabled" Value="False" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="CalendarBody" TargetType="CalendarItem">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CalendarItem">
          <ControlTemplate.Resources>
            <!--
              Mo Tu We across the top. CalendarItem looks this up by a
              ComponentResourceKey of its own rather than by a name, so the key
              has to be spelled out exactly; get it wrong and the lookup is
              guarded, so the headers quietly fall back to unstyled text rather
              than failing loudly.
            -->
            <DataTemplate
              x:Key="{ComponentResourceKey TypeInTargetAssembly={x:Type primitives:CalendarItem},
                      ResourceId=DayTitleTemplate}">
              <TextBlock Text="{Binding}" HorizontalAlignment="Center"
                         VerticalAlignment="Center" FontSize="11" FontWeight="SemiBold"
                         Foreground="{DynamicResource TextSecondary}" />
            </DataTemplate>
          </ControlTemplate.Resources>
          <Grid x:Name="PART_Root">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <Button x:Name="PART_PreviousButton" Grid.Column="0"
                      Style="{StaticResource CalendarNavButton}" Content="&#xE76B;" />
              <Button x:Name="PART_HeaderButton" Grid.Column="1"
                      Foreground="{DynamicResource TextPrimary}" FontSize="13"
                      FontWeight="SemiBold" Background="Transparent" BorderThickness="0"
                      Padding="6,4" HorizontalAlignment="Center">
                <Button.Template>
                  <ControlTemplate TargetType="Button">
                    <Border x:Name="HeaderRoot" Background="Transparent" CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                      <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="HeaderRoot" Property="Background"
                                Value="{DynamicResource List.HoverBackground}" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Button.Template>
              </Button>
              <Button x:Name="PART_NextButton" Grid.Column="2"
                      Style="{StaticResource CalendarNavButton}" Content="&#xE76C;" />
            </Grid>

            <Grid x:Name="PART_MonthView" Grid.Row="1" Visibility="Visible">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
            </Grid>

            <Grid x:Name="PART_YearView" Grid.Row="1" Visibility="Hidden">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>
            </Grid>
          </Grid>

          <ControlTemplate.Triggers>
            <DataTrigger
              Binding="{Binding DisplayMode,
                        RelativeSource={RelativeSource AncestorType=Calendar}}"
              Value="Year">
              <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
              <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger
              Binding="{Binding DisplayMode,
                        RelativeSource={RelativeSource AncestorType=Calendar}}"
              Value="Decade">
              <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden" />
              <Setter TargetName="PART_YearView" Property="Visibility" Value="Visible" />
            </DataTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ThemedCalendar" TargetType="Calendar">
    <Setter Property="Background" Value="{DynamicResource SideBar.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
    <Setter Property="CalendarDayButtonStyle" Value="{StaticResource CalendarDay}" />
    <Setter Property="CalendarButtonStyle" Value="{StaticResource CalendarMonthButton}" />
    <Setter Property="CalendarItemStyle" Value="{StaticResource CalendarBody}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Calendar">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{DynamicResource Panel.Border}" BorderThickness="1"
                  CornerRadius="{StaticResource Radius.Control}" Padding="8">
            <StackPanel x:Name="PART_Root" HorizontalAlignment="Center">
              <CalendarItem x:Name="PART_CalendarItem"
                            Style="{TemplateBinding CalendarItemStyle}" />
            </StackPanel>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- The typed half. Stock DatePickerTextBox brings its own watermark and
       chrome, so it is templated down to the same box as InputTextBox. -->
  <Style x:Key="DateEntryBox" TargetType="DatePickerTextBox">
    <Setter Property="Foreground" Value="{DynamicResource Input.Foreground}" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="Padding" Value="10,7" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="DatePickerTextBox">
          <Grid>
            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                          VerticalAlignment="Center" />
            <TextBlock x:Name="Watermark" Margin="{TemplateBinding Padding}"
                       VerticalAlignment="Center" IsHitTestVisible="False"
                       FontSize="13" Text="Pick a day"
                       Foreground="{DynamicResource Editor.Placeholder}"
                       Visibility="Collapsed" />
          </Grid>
          <ControlTemplate.Triggers>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Text" Value="" />
                <Condition Property="IsKeyboardFocusWithin" Value="False" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Watermark" Property="Visibility" Value="Visible" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="DateBox" TargetType="DatePicker">
    <Setter Property="Background" Value="{DynamicResource Input.Background}" />
    <Setter Property="Foreground" Value="{DynamicResource Input.Foreground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource Input.Border}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CalendarStyle" Value="{StaticResource ThemedCalendar}" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="DatePicker">
          <Border x:Name="Root" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource Radius.Control}"
                  SnapsToDevicePixels="True">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0"
                                 Style="{StaticResource DateEntryBox}"
                                 Focusable="{TemplateBinding Focusable}" />

              <Button x:Name="PART_Button" Grid.Column="1" Width="32"
                      Focusable="False" Background="Transparent" BorderThickness="0"
                      Foreground="{DynamicResource TextSecondary}"
                      FontFamily="Segoe MDL2 Assets" FontSize="13" Content="&#xE787;"
                      ToolTip="Pick from a calendar">
                <Button.Template>
                  <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonRoot" Background="Transparent" CornerRadius="4"
                            Margin="0,2,2,2">
                      <ContentPresenter HorizontalAlignment="Center"
                                        VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="ButtonRoot" Property="Background"
                                Value="{DynamicResource List.HoverBackground}" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Button.Template>
              </Button>

              <Popup x:Name="PART_Popup" Grid.Column="0" StaysOpen="False"
                     Placement="Bottom" AllowsTransparency="True"
                     PlacementTarget="{Binding ElementName=Root}" />
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
              <Setter TargetName="Root" Property="BorderBrush"
                      Value="{DynamicResource Focus.Border}" />
              <Setter TargetName="Root" Property="BorderThickness" Value="2" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Root" Property="Background"
                      Value="{DynamicResource ControlBackgroundDisabled}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!--
    What an album is looking for: the dates, the people, the places. Keyed rather
    than typed, because the panel that edits an existing album's rule and the one
    that describes a new album are two hosts for the same three questions, and
    both bind to the same view model.

    Written once for a reason beyond length: a rule gaining a fourth part has to
    appear in both panels or they quietly disagree about what an album can be -
    and the one that forgot would silently drop whatever the other had set.
  -->
  <DataTemplate x:Key="AlbumRuleFields">
    <StackPanel>
      <!-- Everything here is ANDed: a photograph has to be inside the dates,
           hold every person named, and have been taken in one of the places. -->
      <TextBlock Style="{DynamicResource FieldLabel}" Text="What it is looking for" />
      <TextBlock Style="{DynamicResource HintText}" Margin="0,0,0,6" TextWrapping="Wrap"
                 Text="All of it has to be true at once. Leave a part empty and it asks nothing about it." />

      <!-- Which date question, asked outright. One filled box used to mean
           "that day onwards" to the rule and "that day" to the reader. -->
      <Border Background="{DynamicResource Input.Background}"
              BorderBrush="{DynamicResource Separator}" BorderThickness="1"
              CornerRadius="{DynamicResource Radius.Control}" Padding="2"
              HorizontalAlignment="Stretch" SnapsToDevicePixels="True">
        <UniformGrid Rows="1">
          <RadioButton Style="{DynamicResource SegmentedToggle}" Content="Any day"
                       ToolTip="Ask nothing about when it was taken"
                       IsChecked="{Binding IsAnyDay, Mode=TwoWay}" />
          <RadioButton Style="{DynamicResource SegmentedToggle}" Content="One day"
                       ToolTip="One day exactly"
                       IsChecked="{Binding IsOneDay, Mode=TwoWay}" />
          <RadioButton Style="{DynamicResource SegmentedToggle}" Content="A range"
                       ToolTip="Between two days, both included"
                       IsChecked="{Binding IsDateRange, Mode=TwoWay}" />
        </UniformGrid>
      </Border>

      <DatePicker Style="{DynamicResource DateBox}" Margin="0,6,0,0"
                  AutomationProperties.Name="The day"
                  SelectedDate="{Binding RuleDay, Mode=TwoWay}"
                  Visibility="{Binding IsOneDay,
                               Converter={StaticResource BoolToVisibility}}" />

      <Grid Margin="0,6,0,0"
            Visibility="{Binding IsDateRange,
                         Converter={StaticResource BoolToVisibility}}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <DatePicker Grid.Column="0" Style="{DynamicResource DateBox}"
                    AutomationProperties.Name="From this day"
                    SelectedDate="{Binding RuleFromDate, Mode=TwoWay}" />
        <TextBlock Grid.Column="1" Margin="8,0,8,0" VerticalAlignment="Center"
                   Style="{DynamicResource HintText}" Text="to" />
        <DatePicker Grid.Column="2" Style="{DynamicResource DateBox}"
                    AutomationProperties.Name="To this day"
                    SelectedDate="{Binding RuleToDate, Mode=TwoWay}" />
      </Grid>
      <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                 Foreground="{DynamicResource Status.Danger}"
                 Text="{Binding RuleProblem}"
                 Visibility="{Binding HasRuleProblem,
                              Converter={StaticResource BoolToVisibility}}" />

      <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="Anyone in it" />
      <TextBlock Style="{DynamicResource HintText}" Margin="0,0,0,4" TextWrapping="Wrap"
                 Text="Any one of the people ticked - a photograph does not have to hold all of them." />
      <StackPanel Visibility="{Binding HasPeopleToPick,
                               Converter={StaticResource BoolToVisibility}}">
        <TextBox Style="{DynamicResource InputTextBox}" Margin="0,0,4,0"
                 AutomationProperties.Name="Type a person's name"
                 ToolTip="Type a name and press Enter to add them, or tick from the list"
                 Text="{Binding PeopleFilter, UpdateSourceTrigger=PropertyChanged}">
          <TextBox.InputBindings>
            <KeyBinding Key="Return" Command="{Binding ChoosePersonCommand}" />
          </TextBox.InputBindings>
        </TextBox>
        <TextBlock Style="{DynamicResource HintText}" Margin="0,2,0,4" TextWrapping="Wrap"
                   Foreground="{DynamicResource Status.Caution}"
                   Text="Nobody in this library goes by that name. A rule can only ask for somebody whose face you have already named."
                   Visibility="{Binding NobodyByThatName,
                                Converter={StaticResource BoolToVisibility}}" />
        <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding ShownPeople}" Focusable="False">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <ToggleButton Style="{DynamicResource CheckRow}" Margin="0,1,0,1"
                              HorizontalAlignment="Stretch"
                              IsChecked="{Binding IsChosen, Mode=TwoWay}">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" FontSize="12.5" />
                    <TextBlock Text="{Binding Caption}" FontSize="11" Margin="8,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Editor.Placeholder}" />
                  </StackPanel>
                </ToggleButton>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
        <TextBlock Style="{DynamicResource HintText}" Margin="0,2,0,0"
                   Foreground="{DynamicResource Status.Info}"
                   Text="{Binding PeopleChosen}" />
      </StackPanel>
      <!-- Why there is no box here: with nobody named there is nothing a typed
           name could resolve to, and a rule asking for a name the library
           cannot match is one that quietly finds nothing. -->
      <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                 Text="Nobody has been named yet, so there is no one to ask for. Name a few faces under People first - a rule finds someone by their face, not by their name."
                 Visibility="{Binding HasPeopleToPick,
                              Converter={StaticResource InverseBoolToVisibility}}" />

      <TextBlock Style="{DynamicResource FieldLabel}" Margin="0,14,0,0" Text="Taken in" />
      <TextBlock Style="{DynamicResource HintText}" Margin="0,0,0,4" TextWrapping="Wrap"
                 Text="Any one of the places ticked - a photograph was only taken in one of them." />
      <StackPanel Visibility="{Binding HasPlacesToPick,
                               Converter={StaticResource BoolToVisibility}}">
        <TextBox Style="{DynamicResource InputTextBox}" Margin="0,0,4,0"
                 AutomationProperties.Name="Type a place name"
                 ToolTip="Type a place and press Enter to add it, or tick from the list"
                 Text="{Binding PlacesFilter, UpdateSourceTrigger=PropertyChanged}">
          <TextBox.InputBindings>
            <KeyBinding Key="Return" Command="{Binding ChoosePlaceCommand}" />
          </TextBox.InputBindings>
        </TextBox>
        <TextBlock Style="{DynamicResource HintText}" Margin="0,2,0,4" TextWrapping="Wrap"
                   Foreground="{DynamicResource Status.Caution}"
                   Text="No photograph in this library was taken anywhere by that name. Places come from the coordinates in the file, not from typing."
                   Visibility="{Binding NowhereByThatName,
                                Converter={StaticResource BoolToVisibility}}" />
        <ScrollViewer MaxHeight="120" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
          <ItemsControl ItemsSource="{Binding ShownPlaces}" Focusable="False">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <ToggleButton Style="{DynamicResource CheckRow}" Margin="0,1,0,1"
                              HorizontalAlignment="Stretch"
                              IsChecked="{Binding IsChosen, Mode=TwoWay}">
                  <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Name}" FontSize="12.5" />
                    <TextBlock Text="{Binding Caption}" FontSize="11" Margin="8,0,0,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Editor.Placeholder}" />
                  </StackPanel>
                </ToggleButton>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
        <TextBlock Style="{DynamicResource HintText}" Margin="0,2,0,0"
                   Foreground="{DynamicResource Status.Info}"
                   Text="{Binding PlacesChosen}" />
      </StackPanel>
      <TextBlock Style="{DynamicResource HintText}" TextWrapping="Wrap"
                 Text="No photograph has a place yet, so there is nowhere to ask for. Places come from the coordinates in the file, and a scan is what works them out."
                 Visibility="{Binding HasPlacesToPick,
                              Converter={StaticResource InverseBoolToVisibility}}" />
    </StackPanel>
  </DataTemplate>

</ResourceDictionary>
